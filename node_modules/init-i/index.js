//去重
const one = (arr) => {
    arr.reduce((brr,item) => {
        brr.indexOf(item) === -1 && brr.push(item)
        return brr
    },[])
}
//排序


let crr = [1,4,8,3,0,5]
crr.sort((a,b)=>{
    return a-b
})


module.exports = {
    one
}